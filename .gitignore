/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo

# local env (.env holds only local SQLite + a dev secret and IS committed so a
# fresh clone runs without manual setup; put real overrides in .env.local)
.env.local
.env.*.local

# prisma sqlite
/prisma/dev.db
/prisma/dev.db-journal

# misc
.DS_Store
*.log
