# Local defaults — committed so a fresh clone runs with no manual setup.
# This is a LOCAL SQLite prototype; AUTH_SECRET below is NOT a real secret.
# For real deployments, override these in .env.local (gitignored).
DATABASE_URL="file:./dev.db"
AUTH_SECRET="beau-dev-secret-change-me-in-production-0c1f"
